replicaCount: 1

image:
  repository: noletengine/python-app
  tag: v1
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080
  targetPort: 5000

ingress:
  enabled: true
  className: nginx
  host: python-app.test.com
  path: /
  pathType: Prefix

containerPort: 5000
