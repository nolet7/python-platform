replicaCount: 1

image:
  repository: noletengine/python-app
  tag: techreliant-202605252246
  pullPolicy: Always

service:
  type: ClusterIP
  port: 8080
  targetPort: 5000

ingress:
  enabled: true
  className: nginx
  host: python-app.test.com
  path: /
  pathType: Prefix

containerPort: 5000
