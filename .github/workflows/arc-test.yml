name: ARC Test

on:
  workflow_dispatch:

jobs:
  test-runner:
    runs-on: python-platform-runners
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Show runner info
        run: |
          echo "Runner is working"
          uname -a
          whoami
          pwd
